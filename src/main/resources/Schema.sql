CREATE TABLE Request " +
                    "(id VARCHAR(20) not NULL, " +
                    " monto Decimal(5,2), " +
                    " monedaOrigen VARCHAR(10), " +
                    " monedaDestino VARCHAR(10)

CREATE TABLE Response " +
                    "(id VARCHAR(20) not NULL, " +
                    " monto Decimal(5,2), " +
                    " montoConTipoCambio VARCHAR(10), " +
                    " monedaOrigen VARCHAR(10)
                    " monedaDestino VARCHAR(10)
                    " tipoCambio VARCHAR(10)
                    " mensajeError VARCHAR(50), " +
